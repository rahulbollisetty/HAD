import useAxiosPrivate from "../hooks/useAxiosPrivate";

const createHid = async (data) => {

    
    try {
       
    }
    catch(err){
        // console.log(err);
    }
}

export default createHid;